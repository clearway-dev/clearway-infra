# ClearWay Infrastructure Environment Configuration
# Copy this file to .env and adjust values as needed

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
POSTGRES_DB=clearway
POSTGRES_USER=clearway
POSTGRES_PASSWORD=clearway_dev_password

# Database port (default: 5432)
DB_PORT=5432

# ==========================================
# PGADMIN CONFIGURATION (Optional)
# ==========================================
# Default credentials for pgAdmin web interface
# Note: Must be a valid email format (not .local domain)
PGADMIN_EMAIL=admin@clearway.dev
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ==========================================
# ENVIRONMENT
# ==========================================
# Options: development, staging, production
ENVIRONMENT=development

# ==========================================
# NOTES
# ==========================================
# - Never commit .env file with real credentials
# - Change default passwords in production
# - Use strong passwords for production environments
# - Consider using secrets management for production
